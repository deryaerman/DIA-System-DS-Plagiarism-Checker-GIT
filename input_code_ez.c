int main() {
  if (start_val > 20 && end_val == 10){
    start_val = start_val -1;
  }
  else if (start_val < 18) {
    start_val = start_val -8;
  }
  else {
    start_val = start_val;
  }
}

int main2() {
  if (start_val > 20 && end_val == 10)
    start_val = start_val -1;
  
  else if (start_val < 18)
    start_val = start_val -8;
   
  else 
    start_val = start_val;
  
}
    
